<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title>Hello Ruby TCP Server!</title>
	</head>
	<body>
		<h1>Hello Ruby TCP Server!</h1>
		<% if @request.is_a?(String) %>
			<p>Received: <%= @request %></p>
		<% elsif @request.is_a?(Array) %>
			<ul>
				<% @request[0].split("\r\n").each do |i| %>
					<li><%= i %></li>
				<% end %>
			</ul>
		<% end %>
		<p>At: <%= Time.now.strftime("%Y-%M-%d %H:%M:%S") %></p>
	</body>
</html>